interface CarModel {
  carId: number;
  brand: string;
  features: string[];
  imageUri: string;
  infoCar: string;
  plate: string;
  rating: number;
  seats: number;
  selfUri: string;
}

export default CarModel;
