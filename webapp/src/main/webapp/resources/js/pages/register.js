const smallSelectorElements = document.querySelectorAll(".form-select-sm");

for (const smallSelectorElement of smallSelectorElements) {
    smallSelectorElement.classList.remove("form-select-sm");
}
